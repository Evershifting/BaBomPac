using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Zenject;

public class BonusSpawner : MonoBehaviour {
    //[Inject]
    //Config _config;
    //[Inject]
    //FieldManager _fieldManager;
    //[Inject]
    //readonly Bonus.Pool _bonusPool;
    //readonly List<Bonus> _bonuses = new List<Bonus>();
    //public void Add(Vector3 position, GameObject prefab)
    //{
    //    var bonus = _bonusPool.Spawn();
    //    bonus.transform.position = position;
    //    _bonuses.Add(bonus);
    //}
    //public void Remove(Bonus enemy)
    //{
    //    _bonusPool.Despawn(enemy);
    //    _bonuses.Remove(enemy);
    //}
    //public void Clean()
    //{
    //    for (int i = _bonuses.Count - 1; i >= 0; i--)
    //    {
    //        _bonusPool.Despawn(_bonuses[i]);
    //        _bonuses.Remove(_bonuses[i]);
    //    }
    //}
    //public void Clear()
    //{
    //    for (int i = _bonuses.Count - 1; i >= 0; i--)
    //    {
    //        Remove(_bonuses[i]);
    //    }
    //}
}
